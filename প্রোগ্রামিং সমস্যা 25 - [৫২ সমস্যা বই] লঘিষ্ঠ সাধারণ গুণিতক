#include<stdio.h>
int main()
{
    int T,i,j,a,b;
    scanf("%d",&T);
    while(T--){
       scanf("%d %d",&a,&b);
       for(i=1;i<=b;i++){
        if((a*i)%b==0){
                printf("LCM = %d\n",a*i);
                break;

        }
       }
    }





return 0;




}
